namespace ELifeRPG.Characters.Application;

/// <summary>Handle for src/Api's central AddMediator(options.Assemblies = [...]) call — see ARCHITECTURE.md §9e.</summary>
public static class AssemblyMarker;
